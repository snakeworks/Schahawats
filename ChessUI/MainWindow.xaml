<Window x:Class="ChessUI.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:ChessUI"
        mc:Ignorable="d"
        Title="Schahawats" Height="720" Width="1000"
        WindowStartupLocation="CenterScreen"
        Background="#302E2B"
        PreviewKeyDown="Window_PreviewKeyDown">

    <Grid MouseDown="Grid_MouseDown">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <!--Column 0-->
        <Image x:Name="BoardGrid" Source="Assets/Board.png" Grid.Column="0"/>
        <UniformGrid x:Name="HighlightGrid" Rows="8" Columns="8" 
                     RenderOptions.BitmapScalingMode="HighQuality"
                     Grid.Column="0">
        </UniformGrid>
        <UniformGrid x:Name="PieceGrid" Rows="8" Columns="8"
                     RenderOptions.BitmapScalingMode="HighQuality"
                     Grid.Column="0">
        </UniformGrid>

        <!--Column 1-->
        <Grid Grid.Column="1">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>
            <StackPanel Grid.Row="0" Orientation="Horizontal" HorizontalAlignment="Center" >
                <Button x:Name="TabPlayButton" Content="PLAY" Style="{StaticResource TabButtonStyle}"/>
                <Button x:Name="TabAnalysisButton" Content="ANALYSIS" Style="{StaticResource TabButtonStyle}"/>
                <Button x:Name="TabPuzzlesButton" Content="PUZZLES" Style="{StaticResource TabButtonStyle}"/>
            </StackPanel>

            <!--PLAY MENU-->
            <Grid x:Name="PlayMenu" Grid.Row="1">
                <StackPanel x:Name="PlayMenu_Buttons">
                    <Button x:Name="NewGameButton" Content="New Game" Style="{StaticResource ButtonStyle}" Click="NewGameButton_Clicked"/>
                </StackPanel>

                <Grid x:Name="PlayMenu_MoveHistoryMenu">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>
                    <TextBlock Foreground="White" Grid.Row="0">
                        MOVE HISTORY
                    </TextBlock>
                    <ScrollViewer Grid.Row="1">
                        <StackPanel>
                            <Grid x:Name="MoveHistoryGrid">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="40"/>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="*"/>
                                </Grid.ColumnDefinitions>
                            </Grid>
                        </StackPanel>
                    </ScrollViewer>
                </Grid>
            </Grid>

            <!--ANALYSIS MENU-->
            <Grid x:Name="AnalysisMenu" Grid.Row="1">

            </Grid>

            <!--PUZZLES MENU-->
            <Grid x:Name="PuzzlesMenu" Grid.Row="1">

            </Grid>

        </Grid>
    </Grid>
    <!--<Viewbox Margin="0,0,306,36">
    </Viewbox>*/-->
</Window>
